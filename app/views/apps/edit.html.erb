<%= render 'form' %>

<nav class="links">
  <%= link_to t('label.show'), @app %> |
  <%= link_to(
      t('label.delete'), @app,
      confirm: t('messages.confirmation'), method: :delete
    ) %> |
  <%= link_to t('label.back'), apps_path %>
</nav>